int sum(int begin, int end)
{
    int sum;

    sum = 0;
    while (begin != end) {
        sum = sum + begin;
        begin = begin + 1;
    }
    return sum;
}

int main()
{
    int x = 1, y = 101;

    write(sum(x, y));

    return 0;
}