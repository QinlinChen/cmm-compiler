int f(int n)
{
  if (n == 1)
    return n;
  else
    return (n * f(n - 1));
}

int main()
{
  int m, result;
  m = read();
  if (m > 1)
    result = f(m);
  else
    result = 1;
  write(result);
  return 0;
}
